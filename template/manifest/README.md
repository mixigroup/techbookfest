# textlint for techbookfest (Re:VIEW)

## How to add npm package for textlint

```
$ docker run -it --rm -v `pwd`:/work -w /work ghcr.io/vvakame/review:5.8 /bin/bash -c "npm install --save-dev [package-name]"
```
