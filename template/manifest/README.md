# textlint for techbookfest (Re:VIEW)

## How to add npm package for textlint

```
$ docker run -it --rm -v `pwd`:/work -w /work vvakame/review:4.2 /bin/bash -c "npm install --save-dev [package-name]"
```
